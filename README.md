## FiveM Prop Placer
A prop placing tool written in TypeScript. UI made through React.

## Installation
Navigate to your `resources` folder and clone the repo. Then do the following

```
cd prop-placer
```

Then:
```
cd client

npm install
npm run build
```

And lastly
```
cd ../ui

npm install
npm run build
```